

export const initialState = {
    isRegistering: false,
    isRegisterd: false,
    error: null
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}

    